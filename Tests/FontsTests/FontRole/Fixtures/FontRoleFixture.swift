import Fonts

enum FontRoleFixture
{
    static let allRoles: [FontRole] =
    [
        .screenTitle,
        .sectionTitle,
        .itemTitle,
        .controlLabel,
        .metadata,
        .status,
        .metric,
        .badge,
        .paragraph,
        .paragraphCaption,
        .symbol
    ]
}
